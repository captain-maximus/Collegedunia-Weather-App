import {HOME_ACTION_TYPES} from '../types';

const INITIAL_STATE = {
  loader: false,
};

export default home = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
